export default function Servicos() {
    return (
        <section id="servicos" className="bg-ebano w-full min-h-[calc(100vh-var(--nav-height))] flex items-center justify-center">
        </section>
    )
}